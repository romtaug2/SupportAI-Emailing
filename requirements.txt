# Scraping & HTTP
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
urllib3>=2.0.0
curl_cffi>=0.7.0              # pour contourner Cloudflare (education.gouv.fr)

# Pour France Travail (rendering JS)
playwright>=1.40.0
# Après install : python -m playwright install chromium

# Data / export
pandas>=2.0.0
openpyxl>=3.1.0

# Progression (optionnel, utilisé dans certains logs)
tqdm>=4.65.0
